name: 'hotel_analytics'
version: '1.0'
config-version: 2

profile: 'hotel_analytics'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hotel_analytics:
    staging:
      +materialized: view